#!/bin/bash
# Thin wrapper for backward compatibility.
# All environment setup is handled by install-deps.sh.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SCRIPT_DIR/../scripts/tools/install-deps.sh" "$@"
