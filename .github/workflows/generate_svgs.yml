name: Generate Mermaid SVGs

on:
  push:
    branches: [ main, develop ]
    paths:
      - '**/*.mmd'
      - '**/*.mermaid'
      - '!**/*.svg'  # Explicitly exclude SVG files to prevent loops
      - '!**/node_modules/**'  # Exclude node_modules
      - '!**/.*/**'  # Exclude hidden directories

# Environment variables for configuration
env:
  MERMAID_PATH: ./risk-map/docs/
  SVGS_PATH: ./risk-map/docs/svgs
  MERMAID_THEME: neutral
  MERMAID_BACKGROUND: white
  COMMIT_MESSAGE_PREFIX: "🎨 Auto-generate"
  MAX_PARALLEL_CONVERSIONS: 5

# Use minimal permissions by default
permissions:
  contents: read

jobs:
  generate-svgs:
    runs-on: ubuntu-latest
    name: Generate SVG Files
    # Only run if not triggered by a bot commit (extra safety against loops)
    if: |
      !contains(github.event.head_commit.message, '[skip ci]') &&
      !contains(github.event.head_commit.message, 'Auto-generate') &&
      github.actor != 'github-actions[bot]' &&
      github.actor != 'dependabot[bot]'
    permissions:
      contents: write  # Need write permission only for this job
    steps:
    - name: Generate token for checkout
      id: generate_token
      uses: actions/create-github-app-token@v1
      with:
        # Use a GitHub App for better security and rate limits
        # You need to create a GitHub App and add these secrets
        app-id: ${{ secrets.MERMAID_BOT_APP_ID }}
        private-key: ${{ secrets.MERMAID_BOT_PRIVATE_KEY }}
      continue-on-error: true

    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        # Use bot token if available, otherwise fall back to GITHUB_TOKEN
        token: ${{ steps.generate_token.outputs.token || github.token }}
        # Fetch full history for accurate change detection
        fetch-depth: 0

    - name: Check for package.json
      id: check-package
      run: |
        if [ -f "package.json" ] && [ -f "package-lock.json" ]; then
          echo "has-package=true" >> $GITHUB_OUTPUT
          echo "✅ Found package.json and package-lock.json - will use npm cache"
        else
          echo "has-package=false" >> $GITHUB_OUTPUT
          echo "📦 No package.json found - will use global install with tool cache"
        fi

    - name: Setup Node.js with npm cache
      if: steps.check-package.outputs.has-package == 'true'
      uses: actions/setup-node@v5
      with:
        node-version: '22.x'
        cache: 'npm'

    - name: Setup Node.js without npm cache
      if: steps.check-package.outputs.has-package == 'false'
      uses: actions/setup-node@v5
      with:
        node-version: '22.x'

    - name: Cache global npm packages
      if: steps.check-package.outputs.has-package == 'false'
      uses: actions/cache@v4
      id: npm-cache
      with:
        path: |
          ~/.npm
          ~/.cache/puppeteer
        key: ${{ runner.os }}-npm-mermaid-cli-v10-${{ hashFiles('.github/workflows/generate-mermaid-svgs.yml') }}
        restore-keys: |
          ${{ runner.os }}-npm-mermaid-cli-v10-
          ${{ runner.os }}-npm-mermaid-cli-

    - name: Install dependencies from package.json
      if: steps.check-package.outputs.has-package == 'true'
      run: |
        npm ci --prefer-offline --no-audit
        echo "Mermaid CLI version: $(npx mmdc --version)"

    - name: Install Mermaid CLI globally
      if: steps.check-package.outputs.has-package == 'false'
      run: |
        # Check if we have a cache hit
        if [ "${{ steps.npm-cache.outputs.cache-hit }}" == "true" ]; then
          echo "📦 Cache hit - verifying Mermaid CLI installation..."
        else
          echo "📦 Cache miss - installing Mermaid CLI..."
        fi
        npm install -g @mermaid-js/mermaid-cli@latest
        echo "Mermaid CLI version: $(npx mmdc --version)"

    - name: Setup Chrome for Puppeteer
      uses: browser-actions/setup-chrome@latest

    - name: Validate directory structure
      run: |
        set -euo pipefail
        
        echo "🔍 Checking directory structure..."
        
        if [ ! -d "${MERMAID_PATH}" ]; then
          echo "::warning::Directory ${MERMAID_PATH} does not exist"
          echo "ℹ️  No Mermaid directory found. Creating it..."
          mkdir -p "${MERMAID_PATH}"
        fi
        
        echo "✅ Directory ${MERMAID_PATH} exists"

    - name: Find and convert Mermaid files
      run: |
        set -euo pipefail
        
        echo "🎨 Finding and converting Mermaid files..."

        # Create output directory
        mkdir -p ${SVGS_PATH}

        # Find all .mmd and .mermaid files (excluding node_modules and hidden directories)
        mapfile -t mermaid_files < <(find ${MERMAID_PATH} \
          -type f \
          \( -name "*.mmd" -o -name "*.mermaid" \) \
          -not -path "*/node_modules/*" \
          -not -path "*/.*/*" \
          2>/dev/null || true)

        if [ ${#mermaid_files[@]} -eq 0 ]; then
          echo "⚠️  No Mermaid files found in ${MERMAID_PATH}"
          echo "no_changes=true" >> $GITHUB_ENV
          exit 0
        fi

        echo "📊 Found ${#mermaid_files[@]} Mermaid file(s) to process"

        conversion_count=0
        updated_files=0
        failed_files=0

        # Puppeteer config for better stability
        puppeteer_config='{"args": ["--no-sandbox", "--disable-setuid-sandbox", "--disable-dev-shm-usage"]}'

        # Get list of changed files in this commit
        changed_files=$(git diff --name-only HEAD HEAD~1 2>/dev/null || true)

        # Convert each file
        for file in "${mermaid_files[@]}"; do
          echo "📄 Processing $file"
          
          # Generate SVG with same name but .svg extension
          output_file="${SVGS_PATH}/$(basename "$file" | sed 's/\.[^.]*$/.svg/')"
          
          # Check if source file was changed in this commit or output doesn't exist
          should_regenerate=false
          if [ ! -f "$output_file" ]; then
            echo "  → SVG doesn't exist yet"
            should_regenerate=true
          elif echo "$changed_files" | grep -q "$(basename "$file")"; then
            echo "  → Source file was modified in this commit"
            should_regenerate=true
          else
            # Use git to check if source is newer than last commit of output
            source_time=$(git log -1 --format="%at" -- "$file" 2>/dev/null || echo "0")
            output_time=$(git log -1 --format="%at" -- "$output_file" 2>/dev/null || echo "0")
            if [ "$source_time" -gt "$output_time" ]; then
              echo "  → Source file is newer than SVG"
              should_regenerate=true
            fi
          fi
          
          if [ "$should_regenerate" = true ]; then
            echo "🔄 Converting $(basename "$file")"
            
            if npx mmdc -i "$file" -o "$output_file" \
              -t "${MERMAID_THEME}" \
              -b "${MERMAID_BACKGROUND}" \
              --puppeteerConfig "${puppeteer_config}" 2>/dev/null; then
              echo "✅ Successfully converted $(basename "$file")"
              conversion_count=$((conversion_count + 1))
              updated_files=$((updated_files + 1))
            else
              echo "❌ Failed to convert $(basename "$file")"
              echo "::error file=$file::Failed to convert $file to SVG"
              failed_files=$((failed_files + 1))
            fi
          else
            echo "⏭️  $(basename "$file") is up to date"
            conversion_count=$((conversion_count + 1))
          fi
        done

        # Clean up orphaned SVGs (SVGs without corresponding source files)
        echo ""
        echo "🧹 Checking for orphaned SVG files..."
        orphaned_count=0
        for svg in ${SVGS_PATH}/*.svg; do
          if [ -f "$svg" ]; then
            base=$(basename "$svg" .svg)
            # Check if corresponding source exists
            if ! find ${MERMAID_PATH} \
              \( -name "${base}.mmd" -o -name "${base}.mermaid" \) \
              -not -path "*/node_modules/*" \
              -not -path "*/.*/*" \
              2>/dev/null | grep -q .; then
              echo "🗑️  Removing orphaned SVG: $(basename "$svg")"
              rm "$svg"
              orphaned_count=$((orphaned_count + 1))
              updated_files=$((updated_files + 1))
            fi
          fi
        done

        echo ""
        echo "📊 Summary:"
        echo "  - Processed: $conversion_count files"
        echo "  - Updated: $updated_files files"
        echo "  - Failed: $failed_files files"
        echo "  - Orphaned SVGs removed: $orphaned_count"
        
        # Set output for next step
        echo "updated_files=$updated_files" >> $GITHUB_ENV
        echo "failed_files=$failed_files" >> $GITHUB_ENV

        if [ $failed_files -gt 0 ]; then
          echo "::error::Failed to convert $failed_files file(s)"
          exit 1
        fi

    - name: Configure Git for bot commits
      if: env.no_changes != 'true' && env.updated_files != '0'
      run: |
        # Use bot identity for commits
        if [ -n "${{ steps.generate_token.outputs.token }}" ]; then
          # If using GitHub App
          git config --local user.email "${{ secrets.MERMAID_BOT_APP_ID }}+mermaid-bot[bot]@users.noreply.github.com"
          git config --local user.name "mermaid-bot[bot]"
        else
          # Fallback to GitHub Actions bot
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
        fi

    - name: Commit and push generated SVGs
      if: env.no_changes != 'true' && env.updated_files != '0'
      run: |
        set -euo pipefail
        
        echo "📦 Committing updated SVG files..."

        # Add the SVG files
        git add ${SVGS_PATH}/

        # Check if there are actually changes to commit
        if git diff --staged --quiet; then
          echo "ℹ️  No changes detected in SVG files"
          echo "no_commit_needed=true" >> $GITHUB_ENV
        else
          echo "📝 Committing generated SVG files..."
          
          # Get list of changed files for commit message
          mapfile -t changed_svg_files < <(git diff --staged --name-only | grep '\.svg$' || true)
          changed_count=${#changed_svg_files[@]}
          
          # Create detailed commit message
          commit_message="${COMMIT_MESSAGE_PREFIX} ${changed_count} SVG file(s) from Mermaid diagrams

          Generated from commit: ${{ github.sha }}
          Branch: ${{ github.ref_name }}
          Triggered by: @${{ github.actor }}

          Files updated:"
          
          # Add file list (limit to 20 files to avoid huge commit messages)
          file_count=0
          for file in "${changed_svg_files[@]}"; do
            if [ $file_count -lt 20 ]; then
              commit_message="$commit_message - $(basename "$file")"
              file_count=$((file_count + 1))
            fi
          done
          
          if [ $changed_count -gt 20 ]; then
            commit_message="$commit_message ... and $((changed_count - 20)) more files"
          fi
          
          commit_message="$commit_message [skip ci]"
          
          git commit -m "$commit_message"

          echo "📤 Pushing SVG files to ${{ github.ref_name }}"
          
          # Push with retry logic
          max_attempts=3
          attempt=1
          while [ $attempt -le $max_attempts ]; do
            if git push; then
              echo "✅ SVG files committed and pushed successfully"
              break
            else
              echo "⚠️  Push failed (attempt $attempt/$max_attempts)"
              if [ $attempt -lt $max_attempts ]; then
                echo "Waiting 5 seconds before retry..."
                sleep 5
                # Pull any new changes
                git pull --rebase
                attempt=$((attempt + 1))
              else
                echo "❌ Failed to push after $max_attempts attempts"
                exit 1
              fi
            fi
          done
        fi

    - name: No updates needed
      if: env.no_changes == 'true' || env.updated_files == '0' || env.no_commit_needed == 'true'
      run: |
        echo "ℹ️  No SVG updates needed"
        echo "All SVG files are already up to date."

  generation-summary:
    runs-on: ubuntu-latest
    name: Generation Summary
    needs: [generate-svgs]
    if: always()
    permissions:
      contents: read
    steps:
    - name: Generate Summary
      run: |
        set -euo pipefail
        
        echo "# 📋 Mermaid SVG Generation Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        generation_result="${{ needs.generate-svgs.result }}"
        
        echo "## Status: $generation_result" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        case "$generation_result" in
          "success")
            echo "✅ **SVG generation completed successfully**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "All Mermaid diagrams have been processed and SVG files have been updated." >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Details:" >> $GITHUB_STEP_SUMMARY
            echo "- Commit: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
            echo "- Branch: \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
            echo "- Triggered by: @${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
            ;;
          "skipped")
            echo "⏭️ **SVG generation was skipped**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Possible reasons:" >> $GITHUB_STEP_SUMMARY
            echo "- No Mermaid files were modified" >> $GITHUB_STEP_SUMMARY
            echo "- Commit contained [skip ci] flag" >> $GITHUB_STEP_SUMMARY
            echo "- Triggered by a bot commit" >> $GITHUB_STEP_SUMMARY
            ;;
          *)
            echo "❌ **SVG generation failed**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Action Required:" >> $GITHUB_STEP_SUMMARY
            echo "1. Check the workflow logs for error details" >> $GITHUB_STEP_SUMMARY
            echo "2. Verify that all Mermaid files have valid syntax" >> $GITHUB_STEP_SUMMARY
            echo "3. Test locally with:" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
            echo "npm install -g @mermaid-js/mermaid-cli" >> $GITHUB_STEP_SUMMARY
            echo "npx mmdc -i your-file.mmd -o test.svg" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
            echo "::error::SVG generation failed"
            ;;
        esac
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "---" >> $GITHUB_STEP_SUMMARY
        echo "*Generated by Mermaid SVG Generation Workflow*" >> $GITHUB_STEP_SUMMARY